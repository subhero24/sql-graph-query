{
	"name": "sql-graph-query",
	"version": "1.0.5",
	"description": "Graph query for sqlite",
	"type": "module",
	"main": "build/index.js",
	"scripts": {
		"test": "uvu source/test",
		"build": "esbuild --bundle source/index.js --format=esm --outfile=build/index.js --minify --analyze",
		"develop": "esbuild --bundle source/index.js --format=esm --outfile=build/index.js --watch --analyze",
		"prepare": "npm run build"
	},
	"keywords": [],
	"author": "Bruno Van Damme",
	"license": "MIT",
	"devDependencies": {
		"esbuild": "^0.17.18",
		"eslint": "^8.39.0",
		"fs-extra": "^11.1.1",
		"prettier": "^2.8.8",
		"sqlite": "^4.2.0",
		"sqlite3": "^5.1.6",
		"uvu": "^0.5.6"
	}
}
